#include <iostream>
using namespace std;

int main()
{
	float a,b,sum,razn,pr,chast;
	cin >> a>>b;
	sum=a*a+b*b;
	razn = a * a - b * b;
	pr = a * a * b * b;
	chast = (a * a) / (b * b);
	cout << "sum=" << sum <<" "<< "razn=" << razn <<" "<< "pr=" << pr <<" "<< "chast=" << chast; 
	return 0;
}
